app            = "turingtrade-keeper"
primary_region = "ord"

[build]

[deploy]
  strategy = "immediate"

[processes]
  app = "node scripts/keeper.js"

[[vm]]
  memory   = "256mb"
  cpu_kind = "shared"
  cpus     = 1
