[build]

app = "turingtrade-agent"
primary_region = "ord"

[deploy]
  strategy = "immediate"

[processes]
  app = "node scripts/agent-bot.js"

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
